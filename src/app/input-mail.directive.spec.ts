import { InputMailDirective } from './input-mail.directive';

describe('InputMailDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMailDirective();
    expect(directive).toBeTruthy();
  });
});
